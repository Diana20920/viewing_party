<div class="jumbotron">
  <h1 class="display-4">Welcome to Movie Party</h1>
  <p class="lead">Explore movies and create a viewing party event for you and your friends to watch a movie together. Get similar movie recommendations and browse some upcoming movies.</p>
  <hr class="my-4">
  <div class="card-body">
    <% if !current_user %>
      <%= button_to "Log In", login_path, method: :get, class: "btn btn-primary btn-lg" %>
      <%= link_to "Register as New User", registration_path, class: "card-link" %>
    <% else %>
      <p>You are currently logged in</p>
      <p>Visit "<%= link_to 'My Dashboard', dashboard_path, class: "card-link" %>" to discover movies.</p>
    <% end %>
  </div>
</div>
